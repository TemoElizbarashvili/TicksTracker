<Window x:Class="ExeTickTracker.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="App Usage Viewer" Height="450" Width="800">
    <DockPanel Margin="8">
        <!-- Top bar: search and minimal filter -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 8">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="0 0 8 0"
                     TextChanged="SearchBox_OnTextChanged" />
            <TextBlock Text="Min minutes:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <TextBox x:Name="MinMinutesBox" Width="60" Text="1"
                     TextChanged="MinMinutesBox_OnTextChanged"/>
        </StackPanel>

        <DataGrid x:Name="UsageGrid"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process"
                                    Binding="{Binding ProcessName}"
                                    Width="*"/>
                <DataGridTextColumn Header="Total Time"
                                    Binding="{Binding TotalTimeFormatted}"
                                    Width="150"/>
                <DataGridTextColumn Header="First Seen"
                                    Binding="{Binding FirstSeenLocal}"
                                    Width="160"/>
                <DataGridTextColumn Header="Last Seen"
                                    Binding="{Binding LastSeenLocal}"
                                    Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
